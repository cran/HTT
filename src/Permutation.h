void Permute(int node, NumericMatrix x, IntegerVector sub_ind, NumericMatrix dmat,
             IntegerVector var_type, std::string teststat, int minbucket, double s, int R, NumericMatrix &perm_stat_mat);
